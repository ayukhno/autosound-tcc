"""#19 bisection v0 (as-is): control — the clear exactly as dialog_panel does it; must crash under cdb. See _repro_common.py."""
from _repro_common import make_tests

test_1_mock_transcript, test_2_attach_clears, test_3_next_panel = make_tests("as-is")
