"""#19 bisection v2 (rows-first): rows out of the chat layout first, widgets dropped after. See _repro_common.py."""
from _repro_common import make_tests

test_1_mock_transcript, test_2_attach_clears, test_3_next_panel = make_tests("rows-first")
